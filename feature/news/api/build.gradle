plugins {
    id("com.android.library")
    id("kotlin-parcelize")
    id("kotlin-android")
}

android {
    namespace("br.com.firstsoft.feature.news.api")
}

dependencies {
    api(projects.core.common)
    api(projects.core.usecase)
}
